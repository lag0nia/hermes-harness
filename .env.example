# Optional local secrets/credentials only. Never commit the matching .env file.
# Keep non-secret paths, schedules, and other settings in deployment config.
# INTEGRATION_API_TOKEN=
# INTEGRATION_CLIENT_SECRET=
